# 学習メモ共有アプリ 計画書

## 1. このアプリについて

自分の学習メモを書きためて整理できて、必要に応じて家族・友人・サークルの仲間などに公開もできるWebアプリです。公開したメモには、見た人がコメントを残すこともできます。

想定する使い方:

- 自分だけの復習ノートとして、非公開でメモを書きためる
- タグ（「数学」「英単語」など）を付けて、メモを分類・整理する
- 誰かに見せたいメモだけを公開にして、共有する
- 公開したメモに、仲間からコメントをもらってやりとりする

初めてのアプリ開発ということもあり、「ログイン」「データを保存する仕組み」「複数人での共有」を、できるだけ自分でゼロから作らずに済む構成を選んでいます（理由は3章で説明します）。

## 2. 主な機能一覧

| 機能 | 内容 |
|---|---|
| ログイン／会員登録 | メールアドレスなどでアカウントを作り、ログインして使う |
| メモの作成・編集・削除 | タイトルと本文を書いて保存、あとから編集・削除できる |
| タグ分類 | メモにタグ（例:「数学」）を付けて、あとで絞り込める |
| 公開／非公開の切り替え | メモごとに「自分だけが見られる」か「みんなが見られる」かを選べる |
| コメント | 公開されているメモに対して、他のユーザーがコメントを書き込める |

## 3. 技術構成とその理由

初心者が最短距離で「ログイン」「データ保存」「複数人での共有」を実現するために、それぞれの分野を得意なサービスに任せる構成にしています。

| 分類 | 選定技術 | これが何か | 選んだ理由 |
|---|---|---|---|
| フロントエンド（画面を作る部分） | Next.js（React） | ブラウザに表示する画面を作るための仕組み | 日本語の学習情報や入門記事が非常に多い。ファイルを1つ作るとそれがそのままページのURLになるため、「どのファイルがどの画面か」が初心者にも分かりやすい |
| バックエンド／データベース／認証 | Supabase | ログイン機能とデータの保存場所（データベース）をまとめて提供してくれるサービス | 会員登録・ログイン機能や、「このメモは公開/非公開」のようなアクセス制御を自分でゼロから作らなくてよい。無料枠があり、個人利用なら十分使える |
| ホスティング（インターネットに公開する場所） | Vercel | 作ったWebアプリを、実際にブラウザからアクセスできる状態にして公開するサービス | GitHubにコードを置くだけで、自動的にインターネット上に公開してくれる。Next.jsと同じ会社が作っており相性が良い。無料枠あり |
| バージョン管理 | GitHub | コードの変更履歴を保存・管理する場所 | 「いつ何を変更したか」を記録でき、間違えたときに元に戻せる。Vercelで公開するためにも必要 |

**この構成が初心者向けである理由**

- 自分でサーバーやログイン機能を作る必要がなく、学ぶ概念の数が少なくて済む
- どのサービスも無料の範囲で始められる
- Next.js・Supabase・Vercelはいずれも利用者が多く、困ったときに日本語の解説記事や質問の答えが見つかりやすい

## 4. データ構造（テーブル設計）

アプリの中でデータをどう保存するか（データベースの設計）です。Supabase（内部ではPostgreSQLというデータベースを使っています）の中に、次の5つの表（テーブル）を作ります。

### テーブル一覧

| テーブル名 | 役割 | 主なカラム（項目） |
|---|---|---|
| profiles | ユーザーの表示名などのプロフィール情報（ログインのメールアドレス・パスワードなどはSupabaseのAuth機能が別途管理） | id（ログインユーザーと同じID）, display_name（表示名）, created_at（作成日時） |
| memos | メモ本体 | id, user_id（投稿した人）, title（タイトル）, content（本文）, is_public（公開/非公開、初期値は非公開）, created_at, updated_at |
| tags | タグの一覧（同じタグ名を使い回すためのマスタ） | id, name（タグ名、重複禁止） |
| memo_tags | どのメモにどのタグが付いているかを表す中間テーブル | memo_id, tag_id |
| comments | 公開メモに対するコメント | id, memo_id（どのメモへのコメントか）, user_id（コメントした人）, content, created_at |

### テーブル同士の関係

- 1人のユーザーは、複数のメモを持てる（profiles 1 → memos 多）
- 1つのメモに複数のタグを付けられ、同じタグを複数のメモで使い回せる（memos ⇔ tags は多対多、memo_tagsが橋渡し役）
- 1つのメモに複数のコメントが付く（memos 1 → comments 多）
- 1人のユーザーは、複数のコメントを書ける（profiles 1 → comments 多）

### なぜタグを別テーブルにするのか

メモの中に直接「数学, 復習」のようにタグを書き込む方法（memosテーブルに配列でタグ名を持たせる方法）もありますが、それだと「数学」と「すうがく」のような表記のゆれが起きたときに、同じタグとして扱えなくなってしまいます。そのため本アプリでは配列は使わず、tagsテーブルでタグ名を一元管理し、memo_tagsテーブルでメモとタグを結びつける方式に統一します。こうすることで、表記ゆれを防げるだけでなく、「このタグが付いたメモを一覧表示する」「タグごとの件数を数える」といった機能も作りやすくなります。

### 公開・非公開を安全に守る仕組み

is_public（公開かどうかの項目）だけに頼らず、Supabaseの「行レベルセキュリティ（RLS）」という機能を必ず設定します。RLSとは、「このデータは誰が読んだり書いたりしてよいか」というルールをデータベース自身に守らせる仕組みです。画面側のプログラムにうっかりミスがあっても、データベース側でブロックされるため、他人の非公開メモが漏れることを防げます。

| テーブル | 読み取れる人 | 書き込める人 |
|---|---|---|
| memos | 公開メモは誰でも／非公開メモは本人のみ | 本人のメモのみ、作成・編集・削除できる |
| comments | 対象メモが公開の場合、または自分のメモの場合のみ | ログイン済みで、かつ対象メモが公開の場合のみ投稿できる。編集・削除は自分のコメントのみ |
| tags | 誰でも見られる | ログイン済みなら追加できる（同じ名前のタグがあれば新規作成せず使い回す） |
| profiles | 誰でも見られる（メモやコメントに表示名を出すため） | 本人のプロフィールのみ更新できる |

### メモを削除したときの連動

メモを削除したときに、そのメモに付いていたタグの関連付けやコメントだけが残ってしまう（孤立してしまう）ことを防ぐため、「メモが削除されたら、関連するmemo_tagsとcommentsも自動的に一緒に削除する」設定（カスケード削除）を入れます。

## 5. 開発の進め方（ステップ）

いきなり全部を作ろうとせず、小さく動くものを作りながら段階的に進めます。

1. **環境準備** — Node.js（プログラムを動かすための土台）のインストール、GitHubアカウントの作成、VS Codeなどのエディタの用意
2. **モック作成** — データベースやログインにはまだ繋がず、画面の中にダミーのメモ・タグ・コメントを直接書き込んで、見た目だけを先に作る
3. **デザインを整える** — 配色・フォント・余白などを調整して、見た目をおしゃれに仕上げる
4. **Supabaseの準備** — Supabaseでプロジェクトを作成し、4章のテーブルを作成、ログイン機能を設定する
5. **メモのCRUD画面作成** — モックのダミーデータを、Supabaseに保存された本物のデータに繋ぎ替える
6. **タグ機能・公開/非公開の切り替え実装**
7. **コメント機能実装**
8. **Vercelで公開** — 作ったアプリをインターネット上からアクセスできるようにする

**モックを先に作る理由**: 最初からログインやデータベースに取り組むと、初心者はつまずきやすく、完成イメージも見えにくくなります。先にダミーデータで画面を作ってしまえば、早い段階で「動いているアプリの見た目」を確認でき、モチベーションを保ちやすくなります。また、デザイン調整をこの段階でまとめて行うことで、「見た目を作る作業」と「データを繋ぐ作業」を分けて、一度に学ぶ内容を減らせます。

## 6. 用語ミニ辞典

| 用語 | かんたんな説明 |
|---|---|
| React | 画面の部品（ボタンや一覧など）を組み合わせてWebページを作るための仕組み |
| Next.js | Reactを使いやすくした、画面作りのための土台（フレームワーク） |
| Supabase | ログイン機能とデータベースをまとめて提供してくれるサービス |
| データベース | データを整理して保存しておく場所。表（テーブル）の形で管理する |
| RLS（行レベルセキュリティ） | データベースに「誰が読み書きしてよいか」のルールを設定する機能 |
| モック | 本物のデータの代わりにダミーデータを使って、見た目だけ先に作ったもの |
| デプロイ | 作ったアプリをインターネット上で誰でもアクセスできる状態にすること |
| GitHub | コードの変更履歴を保存・管理する場所 |
| Vercel | Next.jsで作ったアプリをインターネットに公開するためのサービス |

## 7. 今後の拡張候補

- スマホアプリ化（同じデータを使って、iPhone/Android向けアプリも作る）
- タグごとのメモ検索・並び替え機能の強化
- 公開範囲を「全体公開」だけでなく「特定の人にだけ共有」できるようにする
